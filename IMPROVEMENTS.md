# Доработки бота NutriAI

## 1. Отложенное уточнение в функции "Ресторан"

### Описание
После получения рекомендаций из ресторана, бот через 5 минут автоматически спрашивает пользователя, воспользовался ли он рекомендациями, и если да - добавляет выбранное блюдо в дневник питания.

### Workflow
1. **Пользователь получает рекомендации** (2-3 блюда с КБЖУ)
2. **Через 5 минут** бот отправляет сообщение: "Воспользовался ли ты моими рекомендациями?"
3. **Два варианта ответа:**
   - ✅ **Да, воспользовался** → показывает список рекомендаций с кнопками
   - ❌ **Нет, выбрал другое** → дружелюбное завершение

4. **При выборе "Да":**
   - Показывает пронумерованный список всех рекомендаций
   - Каждая с кратким названием и полными КБЖУ
   - Пользователь нажимает кнопку с номером (1, 2, или 3)

5. **Автоматическое добавление в дневник:**
   - Блюдо добавляется в соответствующий прием пищи
   - Показывается обновленный прогресс по дню
   - Учитываются калории и макросы

### Технические детали
- **Формат рекомендаций**: Claude возвращает JSON с структурированными данными
- **Хранение**: Рекомендации сохраняются в `context.user_data`
- **Отложенное сообщение**: `job_queue.run_once(restaurant_followup_callback, when=300)`
- **Новые handlers**:
  ```python
  restaurant_followup_callback()         # Отправка уточнения
  handle_restaurant_used_response()      # Обработка ответа
  handle_restaurant_dish_selection()     # Добавление в дневник
  ```

### Файлы
- `app/bot/handlers/restaurant.py` - основная логика (+240 строк)
- `app/bot/main.py` - регистрация callback handlers
- `app/services/claude_ai.py` - изменен формат ответа на JSON

### Пример JSON ответа от Claude:
```json
{
  "recommendations": [
    {
      "number": 1,
      "name": "Салат Цезарь",
      "calories": 450,
      "proteins": 30,
      "fats": 25,
      "carbs": 20,
      "explanation": "Сбалансированное блюдо с хорошим содержанием белка..."
    },
    {
      "number": 2,
      "name": "Гриль из курицы",
      "calories": 380,
      "proteins": 45,
      "fats": 15,
      "carbs": 10,
      "explanation": "Отличный источник белка..."
    }
  ],
  "general_advice": "С учетом твоего прогресса..."
}
```

---

## 2. Детекция неподходящего контента при анализе фото

### Описание
Бот теперь проверяет фотографии на наличие неподходящего контента (несъедобные предметы, неэтичный материал) перед анализом еды и реагирует соответствующим образом.

### Что проверяется
- ❌ **Несъедобные предметы**: фекалии, мусор, отходы
- ❌ **Неэтичный контент**: изображения насилия, трупов
- ❌ **Нерелевантный контент**: что-то явно не связанное с едой

### Реакция бота
При обнаружении неподходящего контента:

```
⚠️ Обнаружен неподходящий контент

Причина: [причина от AI]

⛔️ Это не смешно и не корректно.

Я создан, чтобы помогать с питанием и здоровьем.
Если у тебя есть проблемы, с которыми нужна помощь,
пожалуйста, обратись к специалисту:

🆘 Экстренная психологическая помощь:
• Телефон доверия: 8-800-2000-122 (бесплатно, круглосуточно)
• Служба поддержки: 8-495-989-50-50

💚 Береги себя. Если что-то беспокоит - обратись за помощью к профессионалам.
```

### Технические детали

#### Обновленный prompt для Claude:
```
ПЕРВАЯ ПРОВЕРКА - Безопасность контента:
Перед анализом еды проверь, содержит ли изображение:
- Несъедобные предметы (фекалии, мусор, отходы и т.д.)
- Неэтичный или шокирующий контент (труп, насилие и т.д.)
- Любой контент, явно не связанный с едой...
```

#### JSON структура ответа:
**При неподходящем контенте:**
```json
{
  "inappropriate_content": true,
  "reason": "обнаружены несъедобные предметы"
}
```

**При нормальном контенте:**
```json
{
  "inappropriate_content": false,
  "dishes": [...],
  "total_nutrition": {...}
}
```

#### Обработка в коде:
```python
if result and result.get("inappropriate_content", False):
    reason = result.get("reason", "неподходящий контент")
    # Показываем предупреждение
    # Логируем: logger.warning(f"Inappropriate content: {reason}")
    return ConversationHandler.END
```

### Файлы
- `app/services/claude_ai.py` - обновлен prompt для анализа фото
- `app/bot/handlers/photo.py` - добавлена проверка и обработка флага

### Логирование
- Все случаи неподходящего контента логируются с WARNING уровнем
- Формат: `"Inappropriate content detected for user {user_id}: {reason}"`
- Позволяет отслеживать попытки злоупотребления

---

## Преимущества доработок

### Для пользователей:
✅ **Удобство**: Не нужно заново фотографировать и вводить данные из ресторана
✅ **Точность**: Рекомендации сразу добавляются с правильными КБЖУ
✅ **Безопасность**: Защита от неподобающего использования бота
✅ **Забота**: Предложение помощи при обнаружении тревожного поведения

### Для разработки:
✅ **Модульность**: Легко расширяемая система handlers
✅ **Логирование**: Полное отслеживание важных событий
✅ **Безопасность**: Proactive защита от abuse
✅ **UX**: Улучшенное взаимодействие с пользователем

---

---

## 3. Верификация распознавания еды с системой коррекций

### Описание
После распознавания еды по фото бот теперь спрашивает пользователя о точности результата и сохраняет коррекции для повторного использования на идентичных фотографиях.

### Workflow
1. **Пользователь отправляет фото еды**
2. **Расчет хеша фото** (SHA256 для идентификации)
3. **Проверка существующих коррекций**:
   - Если найдена → использует сохраненные данные
   - Если не найдена → запрашивает AI-анализ
4. **Показ результатов** с индикатором источника данных
5. **Вопрос верификации**: "✅ Всё распознано верно?"
6. **Два варианта ответа:**
   - ✅ **Да, всё верно** → переход к выбору намерения (есть/узнать)
   - ✏️ **Нужны уточнения** → запрос текстового описания

7. **При выборе "Нужны уточнения":**
   - Пользователь описывает текстом, что на фото
   - AI повторно анализирует с учетом описания
   - Коррекция сохраняется в БД
   - Показываются обновленные результаты
   - Переход к выбору намерения

### Система коррекций

#### База данных:
**Таблица `food_recognition_corrections`:**
- `id` - уникальный идентификатор
- `user_id` - привязка к пользователю
- `photo_hash` - SHA256-хеш фото
- `original_recognition` - исходное AI-распознавание (JSON)
- `corrected_data` - скорректированные данные (JSON)
- `user_clarification` - текст уточнения пользователя
- `created_at` - время создания
- `times_used` - счетчик использований

**Индексы для производительности:**
- `idx_food_corrections_user_id` - по пользователю
- `idx_food_corrections_photo_hash` - по хешу
- `idx_user_photo_hash` - композитный (user_id, photo_hash)

#### Логика работы:
```python
# 1. Расчет хеша
photo_hash = FoodCorrectionService.calculate_photo_hash(image_bytes)

# 2. Поиск существующей коррекции
correction = await FoodCorrectionService.find_correction_by_hash(
    session, user_id, photo_hash
)

# 3. Использование сохраненных данных или AI-анализ
if correction:
    result = correction.corrected_data
    await FoodCorrectionService.increment_usage(correction.id)
else:
    result = await claude_service.analyze_food_photo(...)
```

### Технические детали

#### Новые состояния FSM:
```python
class FoodAddStates(IntEnum):
    ASKING_INTENTION = auto()
    ASKING_VERIFICATION = auto()    # Спрашиваем: распознано верно?
    ASKING_CLARIFICATION = auto()   # Запрашиваем текстовое уточнение
    WAITING_MEAL_TYPE = auto()
```

#### Новые handlers:
```python
handle_verification()      # Обработка ответа "верно/не верно"
handle_clarification()     # Обработка текстового уточнения
```

### Файлы

**Новые:**
- `app/models/food_correction.py` - модель FoodRecognitionCorrection
- `app/services/food_correction_service.py` - сервис управления коррекциями
- `migrations/create_food_recognition_corrections.sql` - миграция БД

**Изменения:**
- `app/bot/handlers/photo.py` - интеграция проверки коррекций (+150 строк)
- `app/bot/main.py` - обновлен ConversationHandler
- `app/bot/states.py` - добавлены новые состояния
- `app/models/user.py` - добавлена связь с food_corrections

### UI Сообщения

**При использовании сохраненной коррекции:**
```
♻️ Использованы данные из предыдущей коррекции

[Результаты распознавания]

✅ Всё распознано верно?
```

**При запросе уточнения:**
```
✏️ Хорошо, давай уточним!

Напиши, что именно на фото и в каком количестве.
Например: "Куриная грудка 200г и салат Цезарь"

📝 Чем подробнее опишешь - тем точнее я распознаю в следующий раз!
```

**После сохранения коррекции:**
```
✅ Отлично! Данные обновлены и сохранены для следующего раза.

[Обновленные результаты]
```

### Преимущества

#### Для пользователей:
✅ **Точность**: Возможность корректировать неправильное распознавание
✅ **Удобство**: Повторные фото распознаются мгновенно и точно
✅ **Персонализация**: Коррекции сохраняются индивидуально для каждого пользователя
✅ **Прозрачность**: Индикатор показывает источник данных (AI или коррекция)
✅ **Контроль**: Верификация запрашивается всегда, даже при использовании коррекций

#### Для разработки:
✅ **Экономия токенов**: Повторные фото не отправляются в Claude API
✅ **Производительность**: Композитный индекс обеспечивает быстрый поиск O(log n)
✅ **Аналитика**: Счетчик использований показывает эффективность коррекций
✅ **Отладка**: Сохранение исходного и скорректированного распознавания
✅ **Масштабируемость**: Легко расширяется для других типов контента

### Метрики

После внедрения можно отслеживать:
- Процент фото, требующих коррекцию
- Частоту повторного использования коррекций (times_used)
- Наиболее частые ошибки распознавания
- ROI от экономии на токенах Claude API

---

## Итоги всех доработок

**Коммиты**: `b59fc7e`, `384050b`
**Ветка**: `claude/session-011CUaCByAYSp9kkzpnHatMp`
**Изменено файлов**: 11
**Добавлено строк**: ~800
**Новых функций**: 6

### Реализованные функции:
1. ✅ Отложенное уточнение в функции "Ресторан"
2. ✅ Детекция неподходящего контента при анализе фото
3. ✅ Верификация распознавания еды с системой коррекций

Все доработки полностью интегрированы, протестированы и готовы к использованию! 🎉
