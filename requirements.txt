# Core Framework
fastapi==0.104.1
uvicorn==0.24.0  # Без [standard] для совместимости с Python 32-bit (избегает httptools)
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (PostgreSQL)
sqlalchemy>=2.0.0  # >= для совместимости с Python 32-bit (greenlet binary wheels)
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.12.1

# Telegram Bot
python-telegram-bot==20.7
APScheduler==3.10.4

# AI - Anthropic Claude
anthropic>=0.40.0

# Image Processing
Pillow>=10.0.0  # >= для совместимости с Python 32-bit (binary wheels)

# PDF Generation
reportlab==4.0.7

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP Client
httpx[socks]==0.25.2  # [socks] для поддержки SOCKS5 прокси (Cloudflare WARP)
aiohttp==3.9.1

# Utils
python-dotenv==1.0.0
pytz==2023.3
python-dateutil==2.8.2
json-repair==0.25.0  # For parsing malformed JSON from AI

# Validation
email-validator==2.1.0

# Logging
loguru==0.7.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Data Processing (для аналитики и статистики)
# ВАЖНО для Windows 32-bit: используйте install-windows.bat или установите с флагом --only-binary :all:
# Указаны минимальные версии (>=) для совместимости с 32-bit Python
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
